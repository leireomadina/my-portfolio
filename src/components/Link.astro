---
interface Props {
	href: string
	title: string
	class?: string
	isExternalLink?: boolean
}

const { href, title, isExternalLink, class: className } = Astro.props
---

<a
	href={href}
	title={title}
	class={className}
	target={isExternalLink ? '_blank' : '_self'}
	rel={isExternalLink ? 'noopener noreferrer' : null}
>
	<slot />
</a>

<style lang="scss" scoped>
	a,
	a:visited {
		color: $color-black;
	}
</style>
